# 設計外包規格（Claude Design Prompt System）

> 這份文件的用途：把「請設計師畫東西」變成一件**可重複、格式固定、拿回來就能落地**的事。
>
> 每次要新的寵物 / 房間場景 / 獎品 / 畫面，都從這裡組裝 prompt，不要重新發明說法。

---

## 怎麼使用這份文件

丟給 Claude Design 的 prompt 一律由四塊組成，順序固定：

```
[區塊 A] 專案前言        ← 每次必貼，不改
[區塊 B] 硬性技術限制    ← 每次必貼，不改
[區塊 C] 資產模板        ← 依這次要什麼，挑一個貼
[區塊 D] 回傳格式        ← 每次必貼，不改
```

拿回結果後，用文末的**驗收清單**檢查，通過才進入實作。

### 已組裝好、可直接複製的 prompt

| 檔案 | 用途 |
|---|---|
| `docs/prompt-pet-husky.md` | 哈士奇 3 個精緻度方案（Track A 起手） |
| `docs/prompt-ui-style.md` | 3 個 UI 風格方向 × quiz/room 兩畫面（Track B 起手） |

整份複製貼進 Claude Design 即可，不用自己組裝。之後要新資產時，照上面四塊自己組一份，
並存成同樣命名慣例的檔案（`docs/prompt-<主題>.md`），下次就不用重寫。

> 註：`docs/design-interactive-pet.md` 是**功能面**的改版計畫（電子雞化玩法），
> 跟這份**視覺外包規格**是兩件事，不要混用。

---

## 區塊 A — 專案前言（每次必貼）

```
這是一個給小學低年級孩子的數學／英文學習 App，叫「寵物小學堂」。
孩子答對題目可以獲得食物和玩具，餵養自己的寵物、把牠養大，最後畢業收藏。

目標裝置：iPad Pro 11 吋，橫向，全螢幕。
使用者：6–9 歲兒童，用手指觸控操作。
氣氛：溫暖、鼓勵、不緊張。答錯不會有懲罰感的紅色警示。

重要：整個 App 的畫面不是網頁，是單一個 HTML canvas，
所有像素都由 JavaScript 的 2D 繪圖指令即時畫出來。
你的設計最後會被人工翻譯成繪圖指令，所以請嚴格遵守下面的技術限制。
```

---

## 區塊 B — 硬性技術限制（每次必貼）

```
## 可以使用
- SVG path（會用 Path2D 直接畫進 canvas，已驗證可行）
- 實色填充、線性漸層、徑向漸層
- 圓角矩形、橢圓、多邊形、貝茲曲線
- 線條描邊（可指定線寬、圓頭/方頭端點）
- 投影（單一顏色 + 模糊半徑 + 位移，不支援 spread）
- 整體透明度

## 不可以使用
- 點陣圖、照片、材質貼圖
- CSS filter、backdrop-blur、mix-blend-mode
- SVG filter（feGaussianBlur 等一律不支援）
- 任何外部資源（字體 CDN、圖片 URL、icon library 的 runtime 引用）
- 新的字體。字體鎖定為 "Andika", "Huninn", "Baloo 2", sans-serif
  （Andika 是識字教學字體，單層 a/g；中文 fallback 到 Huninn）

## 座標與尺寸
邏輯畫布固定 1194 × 834（= iPad Pro 11" 橫向的邏輯解析度）。
1 個畫布單位 = 1 pt，所以你標的座標可以 1:1 抄進程式，請標精確數字。
畫布外緣填色 #E8D9C8。

## 觸控
使用者是 6–9 歲兒童用手指操作。
可點擊元素高度不得小於 66，現有慣例是 84 / 96 / 100。
```

---

## 區塊 C — 資產模板（挑一個貼）

### C1 — 寵物物種

```
## 任務：設計一個「__物種名稱__」的寵物角色

### 座標系
原點在角色的水平中心。
腳底（站立地面）固定在 y = +146。
角色總高不得超過 366（含最高的耳朵），亦即 y 範圍約 -220 ~ +146。
水平範圍約 x = -110 ~ +110。

### 必要變體（缺一不可，都要在同一份設計裡示範）
1. 三個視角：
   - front  正面（預設，停下／吃／玩時使用）
   - side   3/4 側面（走動時使用；預設朝右，程式會鏡射成朝左）
   - back   背面（往房間深處走時使用；看得到屁股、尾巴、耳朵背面，沒有五官）
2. 三個成長階段（程式用整體縮放處理，你只要確認造型在三個尺寸都成立）：
   - 幼幼 ×0.85（另加一根呆毛）
   - 小寶 ×1.0
   - 大寶 ×1.12（另戴一款配件）
   縮放的錨點是 y = 146（腳底），不是中心。
3. 大寶配件 5 款：帽子、髮飾、圍巾這類戴在頭部或頸部的裝飾。
   請一併標出配件的掛載座標，因為配件是獨立繪製、疊在角色上的。

### 動態
角色會被程式做擠壓拉伸（呼吸、開心、咀嚼）與整體旋轉（開心轉圈）。
請確保造型是「一團可以被壓扁拉長」的結構，不要有依賴精確比例才成立的細節。

### 現有 10 個物種（新增物種時避免撞型）
兔兔 rabbit｜倉倉 hamster｜斑斑 tabby（虎斑貓）｜蒙蒙 meerkat（狐獴）｜
豚豚 capybara（水豚）｜哈哈 husky（哈士奇）｜象象 elephant｜橘橘 xmascat（聖誕橘貓）｜
小雞 chick｜貓頭鷹 owl
```

### C2 — 房間場景背景

```
## 任務：設計「__物種名稱__」的房間場景背景

### 座標系
繪製在 440 × 340 的座標空間裡（程式會等比縮放去填滿實際房間框）。

### 版面切分（硬性）
- y = 0 ~ 160（上方 47%）：背景／牆面／天空。這一段可以放遠景。
- y = 160 ~ 340（下方 53%）：地面。寵物會在這裡走動。
- y = 160 附近是「牆與地的分界線」，必須看得出來，寵物的深度感靠它。

### 硬性規則
- **絕對不可以有前景遮擋物**。寵物會在地面區域自由走動，任何畫在牠前面的
  東西都會穿幫。所有物件都必須在寵物身後。
- 地面區域中央偏上會被程式蓋上兩塊地墊（食物區／遊戲區，各約 280×78 的橢圓），
  請不要在那個位置放重要細節。
- 場景要能承受不同亮度的寵物疊上去，避免大面積的高彩度色塊搶掉主角。

### 風格參考（現有的 4 個場景，新場景要跟它們是同一個世界）
- 象象：向日葵花園，青綠漸層底、葉片散佈、向日葵與雛菊
- 橘橘：聖誕紅底、冬青枝、金色四角星
- 小雞：稻草農場，米黃底、木柵欄、彩旗、程序化散佈的稻草
- 貓頭鷹：黃昏森林，四段漸層天空、徑向月暈、視差山丘、松樹、螢火蟲

### 這個物種的既有主題色（場景要與它協調）
wall: ____  dot: ____  accent: ____  deep: ____
```

### C3 — 食物道具

```
## 任務：設計食物道具

### 座標系
原點在正中央，整體不超過 72 × 72（約 x, y 各 ±36）。
會被程式以不同倍率縮放使用（背包格子裡很小，豐收畫面裡很大），
所以造型要在 30px 到 200px 都認得出來。

### 每個食物要兩個版本
1. basic  — 一般版，單份的份量
2. deluxe — 豪華版，同一個食物的「盛大」版本（更多份、加盤子、加裝飾）

註：還有一個金色版本，是程式自動把 basic 版鍍金 + 加閃星產生的，
    你不用設計，但請確保 basic 版鍍金後輪廓還認得出來。

### 現有 14 個食物
蘋果 apple｜橘子 orange｜草莓 strawberry｜香蕉 banana｜蛋糕 cake｜
珍奶 boba｜雞蛋糕 eggcake｜壽司 sushi｜披薩 pizza｜漢堡 burger｜
薯條 fries｜小籠包 bao｜聖代 sundae｜冰淇淋球 scoop
```

### C4 — 玩具道具

```
## 任務：設計玩具道具

### 座標系
原點在正中央，整體不超過 120 × 120（約 x, y 各 ±60）。
同樣會被大幅縮放，30px 到 200px 都要認得出來。

### 每個玩具要兩個版本
1. basic  — 一般版
2. deluxe — 豪華版（更大、更多配件、加光澤）

### 現有 10 個使用中的玩具
小汽車 car｜小火車 train｜玩具廚房 kitchen｜遙控車 rccar｜小飛機 plane｜
布偶熊 teddy｜小機器人 robot｜小火箭 rocket｜玩具屋 dollhouse｜旋轉木馬 carousel
```

### C5 — UI 畫面 / 元件

```
## 任務：設計 __畫面名稱__

### 尺寸
1194 × 834，全畫面。

### 現有畫面（共 22 個，這次要處理的是 ____）
首頁 home｜房間 room｜數學關卡地圖 map｜答題 quiz｜結算 result｜
豐收 feast｜英文地圖 emap｜字母選單 emenu｜英文答題 eplay｜英文結算 eresult｜
描寫練習 epractice｜手寫過關 hwpass｜圖鑑 dex｜獎品商店 shop｜
選寵物 pickpet｜畢業 graduate｜珍藏館 museum｜換裝 dressup｜好友拜訪 visit

### 產出要求
請標明每個元素的精確座標、尺寸、圓角半徑、顏色（hex）、字級。
這些數字會被直接抄進程式。
```

---

## 區塊 D — 回傳格式（每次必貼）

```
## 請用這個格式交付

除了視覺稿之外，請附上可直接轉成程式的資料：

### 1. 繪製清單（依繪製順序，由後往前）
每一項給我：
- d          SVG path 的 d 字串（座標就用上面指定的座標系，不要另外正規化）
- fill       填色 hex 或漸層定義
- stroke     描邊色（沒有就省略）
- lineWidth  線寬（沒有描邊就省略）
- opacity    透明度（1 就省略）

漸層請寫成：
- 線性：起點 (x,y)、終點 (x,y)、色停站列表
- 徑向：圓心 (x,y)、起始半徑、結束半徑、色停站列表

### 2. 色票
這份設計用到的所有顏色，列成一張表，每個顏色給一個語意化的名字
（例如 wall-base / fur-shadow / accent-warm），不要只給 hex。

### 3. 風格規則
這份設計的內在規則是什麼？請明確寫出數字：
- 線寬有哪幾種？
- 圓角半徑有哪幾階？
- 有沒有統一的描邊？
- 陰影的參數是什麼？
這一段最重要 —— 之後所有新資產都要遵守同一套規則，所以規則必須是可執行的
數字，不能是「溫暖」「圓潤」這種形容詞。

### 4. 取材來源（如果有）
若參考了免費授權素材庫，請標明來源與授權條件。
```

---

## 驗收清單（我用的，不貼給設計端）

拿回設計後逐項確認，有任何一項不過就退回重做，不要硬吞：

- [ ] 每個形狀都能表達成 `Path2D` 吃得下的 `d` 字串
- [ ] 沒有用到 filter / blur / blend mode / 點陣材質
- [ ] 沒有引入新字體
- [ ] 座標用的是指定座標系，不是 0–1 正規化或任意畫布尺寸
- [ ] 顏色是明確的 hex 或 rgba，不是「淺藍色」
- [ ] 寵物：三視角齊全、三階段縮放成立、5 款配件含掛載座標
- [ ] 場景：440×340、牆地分界在 y≈160、無前景遮擋、地墊區留白
- [ ] 道具：basic + deluxe 兩版、在 30px 縮圖下仍可辨識
- [ ] 風格規則那一段給的是數字，不是形容詞
- [ ] 可點擊元素高度 ≥ 66

---

## 附錄一：現有物種主題色

| 物種 | id | wall | dot | accent | deep | 專屬場景 |
|---|---|---|---|---|---|---|
| 兔兔 | rabbit | `#FBE8E4` | `rgba(238,170,170,.25)` | `#B97A82` | `#8A5560` | ✗ |
| 倉倉 | hamster | `#E6EFF5` | `rgba(140,175,205,.22)` | `#6E8BA4` | `#4E6B84` | ✗ |
| 斑斑 | tabby | `#FBEEDD` | `rgba(230,170,110,.22)` | `#C58A4E` | `#8A5A2E` | ✗ |
| 蒙蒙 | meerkat | `#F0E4CD` | `rgba(190,150,95,.20)` | `#B26A3C` | `#7A4526` | ✗ |
| 豚豚 | capybara | `#DCE8E0` | `rgba(120,160,140,.20)` | `#5E8A72` | `#3E5F4E` | ✗ |
| 哈哈 | husky | `#E2E9F2` | `rgba(120,150,190,.20)` | `#3E9E8E` | `#33506E` | ✗ |
| 象象 | elephant | `#CFE9E1` | `rgba(110,175,155,.20)` | `#3F9E8C` | `#2E6E60` | ✓ 向日葵花園 |
| 橘橘 | xmascat | `#C24E5A` | `rgba(244,198,78,.22)` | `#2E7D46` | `#7A2530` | ✓ 聖誕冬青 |
| 小雞 | chick | `#F6E9CC` | `rgba(211,166,63,.18)` | `#C99A4E` | `#8A6242` | ✓ 稻草農場 |
| 貓頭鷹 | owl | `#E3E1EF` | `rgba(91,107,166,.18)` | `#5B6BA6` | `#3E3A66` | ✓ 黃昏森林 |

## 附錄二：程式端對應位置

| 資產類型 | 檔案 | 進入點 |
|---|---|---|
| 寵物 | `app/pets.js` | `draw(species, ctx, t, {stage, dir, growDeco})`；配件在 `xxxDeco(ctx, i, fx)` |
| 房間場景 | `app/screens.js` | `window.PLS_SCENE_ROOM[species]`；通用房間在 `app/room.js` 的 `roomInterior()` |
| 食物 | `app/art.js` | `FOODS` / `FOODS_DELUXE`；鍍金由 `drawFoodGold()` 自動處理 |
| 玩具 | `app/toys.js` | `TOYS`；豪華版 `drawToyDeluxe()` |
| 共用繪圖原語 | `app/art.js` | `window.PLS_ART`：`el` `rr` `pill` `bubble` `sparkle` `heart` `fitText` `wrapLines` |
| 字母骨架 | `app/letters.js` | `GLYPHS` 表（**SVG path 進 canvas 的既有範例**，見 `app/letters.js:169`） |
| 畫布框架 | `app/main.js` | `W=1194, H=834`；`PLS.addButton({x,y,w,h,draw,onTap})` |

## 附錄三：色彩現況

程式中目前有 **515 個相異的 hex 色值**，沒有 token 層。其中重複度最高的幾個：

`#FFFFFF`×68、`#8A6242`×29、`#F6C95E`×26、`#8FC9A8`×23、`#F2B96B`×21、
`#9A7B5C`×19、`#F4C64E`×17、`#5E4A36`×17

注意 `#F6C95E` / `#F4C64E` / `#F6C44A` 是三個幾乎一樣的金色，各自被用了 26 / 17 / 9 次
—— 這正是缺 token 層的症狀。設計系統定案後會收斂進 `app/theme.js`。
